# myproteinDailyDiscount
Get myprotein discount on a mail every day. This repository create cron job.

# Prerequirements

- Nodejs
- Puppeteer (npm install --save puppeteer)
- Nodemailer (npm install --save nodemailer)
- Linux

